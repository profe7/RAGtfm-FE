services:
  frontend:
    build: .
    container_name: ragtfm-frontend
    ports:
      - "3000:80"
    networks:
      - ragtfm-network

networks:
  ragtfm-network:
    external: true
    name: ragtfm-be_default